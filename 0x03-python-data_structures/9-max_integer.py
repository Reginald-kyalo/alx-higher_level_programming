#!/usr/bin/python3
def max_integer(my_list=[]):
    if len(list) == 0:
        return
    temp = 0
    for i in range(len(my_list)):
        if my_list[i] > temp:
            temp = my_list[i]
    return temp 
